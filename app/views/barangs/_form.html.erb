<%= form_with(model: barang) do |form| %>
  <% if barang.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(barang.errors.count, "error") %> prohibited this barang from being saved:</h2>

      <ul>
        <% barang.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nama, 'Nama Barang', style: "display: block" %>
    <%= form.text_area :nama %>
  </div>

  <div>
    <%= form.label :jenis, 'Jenis Barang' ,style: "display: block" %>
    <%= form.text_field :jenis %>
  </div>

  <div>
    <%= form.label :bahan, style: "display: block" %>
    <%= form.text_field :bahan %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
